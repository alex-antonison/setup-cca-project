# need to reset ambari admin password - default is hadoop
# This gets you into the happy base
ssh root@127.0.0.1 -p 2222
ambari-admin-password-reset

# How to setup a tunnel
nohup ssh -i "aws-boinc-key.pem" -N -L 8080:ec2-35-153-134-7.compute-1.amazonaws.com:8080 ubuntu@ec2-35-153-134-7.compute-1.amazonaws.com &

# If you stop instance
# Do this to reboot it



# Python to test if it works
import happybase as hb
con = hb.Connection()
con.create_table('mytable', {'cf': {}})